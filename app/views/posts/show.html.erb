<p id="nav-link"><%= link_to 'Edit', edit_post_path(@post) %> | <%= link_to 'Back', posts_path %></p>

<p id="notice"><%= notice %></p>

<div>
	<p>
	  <strong>Title:</strong>
	  <%= @post.title %>
	  <%= rand(1..10).downto(1).inject(:*) %>
	</p>

	<p>
	  <strong>Body:</strong>
	  <%= @post.body %>
	</p>

	<p>
	  <strong>Published:</strong>
	  <%= @post.published %>
	</p>

	<%= link_to 'Edit', edit_post_path(@post) %> |
	<%= link_to 'Back', posts_path %>
</div>